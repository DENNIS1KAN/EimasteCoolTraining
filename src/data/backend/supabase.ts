// Placeholder: implemented in the Supabase phase.
import type { AppConfig } from '../../config'
import { LocalBackend } from './local'

export class SupabaseBackend extends LocalBackend {
  constructor(_config: AppConfig) {
    super()
  }
}
